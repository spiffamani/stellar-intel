{
  "name": "@stellarintel/publisher",
  "version": "0.1.0",
  "description": "Stellar Intel off-chain publisher - reads outcomes from Postgres and submits batches to the reputation oracle",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26",
    "typescript": "^6",
    "vitest": "^4"
  },
  "files": ["dist"]
}
